/**
 * Shadow configuration for point lights.
 * Uses a cube shadow map approach with 6 perspective cameras (one per direction).
 * This allows omnidirectional shadow casting from point lights.
 * @todo
 */
function UePointLightShadow(data = {}) constructor {
    
}
