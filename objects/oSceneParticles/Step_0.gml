//time += 0.1;
//
//// --- Flickering Light ---
//if (variable_instance_exists(self, "fireLight")) {
    //var flicker = 1.0 + random_range(-0.2, 0.2);
    //fireLight.intensity = 4.0 * flicker;
    //
    //// Slight movement for the light
    //fireLight.position[VEC3.x] = random_range(-0.05, 0.05);
    //fireLight.position[VEC3.z] = random_range(-0.05, 0.05);
//}
//
//// --- Scene Update ---
//var dt = delta_time / 1000000; // Assuming 60 FPS for now, can use delta_time / 1000000
//fireSystem.update(dt, camera);
//smokeSystem.update(dt, camera);
//
//renderer.render(scene, camera);
//orbitControls.update();
